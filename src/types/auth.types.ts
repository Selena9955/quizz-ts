export type AuthMessage = {
  email: string;
  password: string;
  secondPassword: string;
  userName: string;
};

export type registerFormData = {
  email: string;
  password: string;
  secondPassword: string;
  username: string;
};

export type LoginFormData = {
  email: string;
  password: string;
};
